{
  "//resolveJsonModule": "src/lib/placeholders.ts reads the generator manifest, and both check scripts reach it through the SEO layer.",
  "//": "Used by `npm run check:content`, `check:publication` and `images:record` — compiles the content engine to CJS so it can run outside Next.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "commonjs",
    "moduleResolution": "node",
    "outDir": ".content-check",
    "rootDir": ".",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "noEmit": false,
    "resolveJsonModule": true,
    "types": ["node"]
  },
  "include": ["scripts/**/*.ts", "src/**/*.ts"],
  "//exclude": "Browser-only layers. The content engine must stay runnable in plain Node.",
  "exclude": [
    "src/app/**",
    "src/lib/mdx/**",
    "src/components/**",
    "src/services/**",
    "src/lib/cn.ts",
    "src/lib/plates.ts"
  ]
}
