/** Single locale for now. Fixed so server and client render identical dates/numbers. */
export const DEFAULT_LOCALE = "en-IN";

/** BCP-47 tag used in `<html lang>` and Open Graph. */
export const DEFAULT_HTML_LANG = "en";

export const DEFAULT_OG_LOCALE = "en_IN";
