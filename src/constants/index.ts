/**
 * Reusable constants.
 *
 * Related, but deliberately elsewhere:
 * - Company, contact and social details → `src/config/site.ts` (one editable file).
 * - Navigation                          → `src/data/navigation` (data, not constants).
 * - Product categories                  → derived from `src/content/categories` at build time.
 */
export * from "./locale";
export * from "./routes";
