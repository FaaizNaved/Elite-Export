export { ErrorState } from "./error-state";
export type { ErrorStateProps } from "./error-state";

/** Empty state lives in the design system — re-exported so failure and empty
 *  states can be imported from one place. */
export { EmptyState } from "@/components/ui/loading";
export type { EmptyStateProps } from "@/components/ui/loading";
